"use client";
import { Download,FileJson,FileSpreadsheet } from "lucide-react";import { PageHeading } from "@/components/page-heading";import { useAppStore } from "@/components/app-store";
export default function Page(){const{exports}=useAppStore();return <><PageHeading eyebrow="Lokale downloads" title="Exportgeschiedenis" description="Een overzicht van bestanden die vanuit deze browser zijn aangemaakt. De bestanden zelf blijven in je downloadmap."/>{exports.length?<div className="card overflow-hidden"><div className="grid grid-cols-[1fr_auto_auto] gap-3 bg-slate-50 px-5 py-3 text-xs font-bold uppercase tracking-[.06em] text-slate-500"><span>Bestand</span><span>Leads</span><span>Datum</span></div>{exports.map(x=>{const Icon=x.format==="json"?FileJson:FileSpreadsheet;return <div key={x.id} className="grid grid-cols-[1fr_auto_auto] items-center gap-3 border-t border-slate-100 px-5 py-4"><div className="flex min-w-0 items-center gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-lg bg-slate-100"><Icon size={18}/></span><div className="min-w-0"><p className="truncate text-sm font-bold">{x.fileName}</p><p className="text-xs uppercase text-slate-500">{x.format}</p></div></div><span className="badge">{x.count}</span><span className="text-xs text-slate-500">{new Date(x.createdAt).toLocaleString("nl-NL")}</span></div>})}</div>:<div className="card grid min-h-72 place-items-center p-8 text-center"><div><span className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-slate-100 text-slate-500"><Download/></span><h2 className="mt-4 font-bold">Nog geen exports</h2><p className="mt-1 max-w-sm text-sm text-slate-500">Selecteer leads in het resultatenoverzicht en kies CSV, Excel of JSON.</p></div></div>}</>}
