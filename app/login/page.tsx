import { redirect } from "next/navigation";
import { CheckCircle2 } from "lucide-react";
import { currentUser } from "@/lib/auth/session";
import { Brand } from "@/components/brand";
import { LoginForm } from "@/components/login-form";
import { ThemeToggle } from "@/components/theme-toggle";
export const dynamic = "force-dynamic";
export default async function LoginPage(){if(await currentUser())redirect("/dashboard");return <main className="login-shell"><section className="login-visual"><Brand/><div className="login-copy"><span className="eyebrow" style={{color:"#72d9b9"}}>Nederland & België</span><h1>Van betrouwbare bedrijfsdata naar echte verkoopkansen.</h1><p style={{color:"#bad3ca",fontSize:17,lineHeight:1.7,maxWidth:520}}>Vind operationele bedrijven zonder website of met een aantoonbare verbeterkans. Centraal opgeslagen, geanalyseerd en klaar voor opvolging.</p><div style={{display:"grid",gap:12,marginTop:30}}>{["Alleen geverifieerde, actieve bedrijven","Automatische incrementele dekking","Heldere pipeline en permanente notities"].map(item=><span key={item} style={{display:"flex",gap:9,alignItems:"center",fontSize:14}}><CheckCircle2 size={17} color="#72d9b9"/>{item}</span>)}</div></div><span className="small" style={{color:"#8eb0a3",position:"relative",zIndex:1}}>leadfindersitora.nl · Beveiligde toegang</span></section><section className="login-panel"><ThemeToggle className="login-theme-toggle"/><div className="login-form"><span className="eyebrow">Welkom terug</span><h2>Log in op Leadfinder</h2><p className="muted">Alleen geautoriseerde gebruikers hebben toegang.</p><LoginForm/><p className="small muted" style={{marginTop:24,lineHeight:1.6}}>Geen account? Neem contact op met de beheerder. Er is bewust geen openbare registratie.</p></div></section></main>}
