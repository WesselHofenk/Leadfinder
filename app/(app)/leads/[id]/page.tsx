import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, ExternalLink, Mail, MapPin, Phone } from "lucide-react";
import { prisma } from "@/lib/prisma";
import { dateFormatter, statusLabels } from "@/lib/format";
import { AnalyzeButton, LeadEditor } from "@/components/lead-actions";
export default async function LeadDetailPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const lead=await prisma.lead.findUnique({where:{id},include:{websiteAnalyses:{orderBy:{createdAt:"desc"},take:1},leadNotes:{orderBy:{createdAt:"desc"},take:20,include:{user:{select:{name:true}}}}}});if(!lead)notFound();const analysis=lead.websiteAnalyses[0];const reasons=(analysis?.reasons as {code:string;label:string;weight:number}[]|undefined)||[{code:"NO_WEBSITE",label:"Geen website gevonden",weight:95}];return <div className="content"><Link href="/leads" className="button button-secondary" style={{marginBottom:20}}><ArrowLeft size={15}/>Terug naar leads</Link><header className="page-head"><div><span className="eyebrow">{statusLabels[lead.leadType]}</span><h1>{lead.companyName}</h1><p className="muted">{lead.category.replaceAll("_"," ")} · {lead.city}, {lead.country}</p></div><div className="actions" style={{display:"flex",gap:8,alignItems:"flex-start",flexWrap:"wrap"}}><a className="button button-secondary" href={lead.googleMapsUrl} target="_blank" rel="noreferrer"><MapPin size={15}/>Google Maps <ExternalLink size={13}/></a>{lead.websiteUrl&&<a className="button button-secondary" href={lead.websiteUrl} target="_blank" rel="noreferrer">Website <ExternalLink size={13}/></a>}<AnalyzeButton leadId={lead.id} disabled={!lead.websiteUrl}/></div></header><section className="stats"><Score label="Opportunity Score" value={lead.opportunityScore} meta="Hoger = interessantere websitekans"/><Score label="Mobiele score" value={analysis?.mobileScore??null} meta="Google PageSpeed / Lighthouse"/><Score label="Desktopscore" value={analysis?.desktopScore??null} meta="Google PageSpeed / Lighthouse"/><Score label="Geschatte conversiekwaliteit" value={lead.conversionQualityScore} meta="Automatische schatting, geen analytics"/></section><div className="detail-grid"><div style={{display:"grid",gap:18}}><section className="card card-pad"><h2>Waarom is dit een kans?</h2><div style={{display:"flex",flexWrap:"wrap",gap:8,marginTop:16}}>{reasons.map(reason=><span className="badge badge-amber" key={reason.code}>{reason.label}</span>)}</div>{analysis&&<dl style={{marginTop:18}}><Row label="Bereikbaar">{analysis.isReachable?"Ja":"Nee"}</Row><Row label="Mobielvriendelijk">{yesNo(analysis.isMobileFriendly)}</Row><Row label="Duidelijke call-to-action">{yesNo(analysis.hasClearCta)}</Row><Row label="Contactformulier">{yesNo(analysis.hasContactForm)}</Row><Row label="Kapotte links">{analysis.hasBrokenLinks?`${analysis.brokenLinkCount} gevonden`:"Geen in steekproef"}</Row><Row label="Serverreactie">{analysis.loadTimeMs?`${analysis.loadTimeMs} ms`:"Niet beschikbaar"}</Row><Row label="Laatste analyse">{dateFormatter.format(analysis.createdAt)}</Row></dl>}</section><section className="card card-pad"><h2>Bedrijfsgegevens</h2><dl><Row label="Contactpersoon">{lead.contactPersonName||"Niet beschikbaar"}</Row><Row label="Telefoon"><span style={{fontWeight:750}}><Phone size={14} style={{display:"inline",marginRight:6}}/>{lead.normalizedPhoneNumber}</span></Row><Row label="E-mail">{lead.email?<span><Mail size={14} style={{display:"inline",marginRight:6}}/>{lead.email}</span>:"Niet beschikbaar"}</Row><Row label="Adres">{lead.streetAddress}</Row><Row label="Postcode en plaats">{lead.postalCode||"—"} {lead.city}</Row><Row label="Gemeente">{lead.municipality||"—"}</Row><Row label="Provincie / regio">{lead.province||"—"}</Row><Row label="Website">{lead.websiteUrl||"Geen website gevonden"}</Row><Row label="Bedrijfsstatus"><span className="badge badge-green">Operationeel</span></Row><Row label="Bron">Google Places · {lead.externalPlaceId}</Row><Row label="Gevonden">{dateFormatter.format(lead.firstDiscoveredAt)}</Row><Row label="Gecontroleerd">{dateFormatter.format(lead.lastVerifiedAt)}</Row></dl></section><section className="card card-pad"><h2>Notitiegeschiedenis</h2>{lead.leadNotes.length?<div style={{display:"grid",gap:10,marginTop:15}}>{lead.leadNotes.map(note=><div key={note.id} style={{padding:"12px 0",borderBottom:"1px solid var(--line)"}}><p style={{whiteSpace:"pre-wrap",marginBottom:6}}>{note.content}</p><span className="small muted">{note.user.name} · {note.createdAt.toLocaleString("nl-NL")}</span></div>)}</div>:<p className="small muted">Nog geen notities toegevoegd.</p>}</section></div><aside style={{display:"grid",gap:18,alignContent:"start"}}><section className="card card-pad"><h2>Pipeline</h2><p><span className={`badge ${lead.status==="INVOICED"?"badge-green":lead.status==="FILTERED"?"badge-amber":"badge-blue"}`}>{statusLabels[lead.status]}</span></p>{lead.filterReason&&<p className="small muted"><strong>Filterreden:</strong><br/>{lead.filterReason}</p>}<p className="small muted">Handmatige status, notities en filterreden worden nooit door een automatische hercontrole overschreven.</p></section><LeadEditor leadId={lead.id} status={lead.status} notes={lead.notes} filterReason={lead.filterReason}/></aside></div></div>}
function Row({label,children}:{label:string;children:React.ReactNode}){return <div className="definition"><dt>{label}</dt><dd>{children}</dd></div>}
function Score({label,value,meta}:{label:string;value:number|null;meta:string}){return <div className="card stat"><span className="stat-label">{label}</span><div className="stat-value">{value??"—"}{value!==null&&<small style={{fontSize:14,color:"var(--muted)"}}>/100</small>}</div><div className="stat-meta">{meta}</div></div>}
function yesNo(value:boolean|null){return value===null?"Niet beschikbaar":value?"Ja":"Nee"}
