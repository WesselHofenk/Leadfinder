"use client";
import Link from "next/link";import { ArrowRight,Bookmark,Building2,Mail,Phone,Search,SquareCheckBig } from "lucide-react";import { PageHeading } from "@/components/page-heading";import { useAppStore } from "@/components/app-store";
export function DashboardView(){const{leads,savedIds,selectedIds,searches}=useAppStore();const stats=[["Gevonden bedrijven",leads.length,"Beschikbaar in de huidige dataset",Building2],["Geselecteerde leads",selectedIds.length,"Klaar voor een bulkactie",SquareCheckBig],["Opgeslagen leads",savedIds.length,"Bewaard in deze browser",Bookmark],["Met e-mailadres",leads.filter(l=>l.email).length,"Direct per e-mail bereikbaar",Mail],["Met telefoonnummer",leads.filter(l=>l.phone).length,"Telefonisch bereikbaar",Phone]] as const;const recentSaved=leads.filter(l=>savedIds.includes(l.id)).slice(0,5);return <><PageHeading eyebrow="Leadfinder-overzicht" title="Vind bedrijven die bij je doelgroep passen" description="Zoek, selecteer en exporteer Nederlandse bedrijfsleads zonder account of installatiestappen." action={<Link href="/zoeken" className="btn btn-primary pressable"><Search size={18}/>Nieuwe zoekopdracht</Link>}/><section aria-label="Kernstatistieken" className="grid grid-cols-2 gap-3 xl:grid-cols-5">{stats.map(([label,value,sub,Icon])=><article key={label} className="card lift pressable min-w-0 p-4"><span className="grid h-9 w-9 place-items-center rounded-lg bg-slate-100 text-[#253a5b]"><Icon size={18}/></span><p className="tabular mt-4 text-2xl font-bold tracking-[-.03em]">{value}</p><p className="mt-1 text-xs font-semibold text-slate-700">{label}</p><p className="mt-1 hidden text-[11px] text-slate-400 sm:block">{sub}</p></article>)}</section><div className="mt-5 grid gap-5 lg:grid-cols-2"><article className="card overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><h2 className="font-bold">Recente zoekopdrachten</h2><p className="text-xs text-slate-500">Uitgevoerd en lokaal opgeslagen</p></div><Link href="/zoeken" className="text-sm font-bold text-[#a95022]">Nieuwe zoekactie</Link></div>{searches.slice(0,5).map(s=><div key={s.id} className="flex items-center gap-3 border-b border-slate-100 px-5 py-3 last:border-0"><span className="grid h-9 w-9 place-items-center rounded-lg bg-slate-100"><Search size={16}/></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{s.query} · {s.city}</p><p className="text-xs text-slate-500">{s.saved?"Opgeslagen filter":`${s.count} resultaten`} · {s.provider}</p></div><span className="text-xs text-slate-400">{new Date(s.date).toLocaleDateString("nl-NL")}</span></div>)}</article><article className="card overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><h2 className="font-bold">Recent opgeslagen leads</h2><p className="text-xs text-slate-500">Direct verder met opvolging</p></div><Link href="/leads" className="flex items-center gap-1 text-sm font-bold text-[#a95022]">Alles bekijken <ArrowRight size={15}/></Link></div>{recentSaved.length?recentSaved.map(l=><Link href={`/leads/${l.id}`} key={l.id} className="flex items-center gap-3 border-b border-slate-100 px-5 py-3 last:border-0 hover:bg-slate-50"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[#fff3ea] text-xs font-bold text-[#a95022]">{l.leadScore}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{l.name}</p><p className="text-xs text-slate-500">{l.branch} · {l.city}</p></div><span className="badge">{l.status}</span></Link>):<div className="p-8 text-center text-sm text-slate-500">Nog geen leads opgeslagen.</div>}</article></div><section className="mt-5 rounded-2xl bg-[#0b1830] p-6 text-white sm:flex sm:items-center sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#efb184]">Zonder account</p><h2 className="mt-1 text-xl font-bold">Je leadfinder is direct klaar voor gebruik</h2><p className="mt-1 text-sm text-slate-300">Voorkeuren, statussen en notities blijven lokaal in je browser bewaard.</p></div><Link href="/zoeken" className="btn pressable mt-4 bg-white text-[#0b1830] sm:mt-0">Start met zoeken <ArrowRight size={17}/></Link></section></>}
