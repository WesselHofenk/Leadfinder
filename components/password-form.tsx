"use client";
import { useState } from "react";
export function PasswordForm(){const[pending,setPending]=useState(false);const[message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);const form=new FormData(event.currentTarget);const response=await fetch("/api/auth/password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const data=await response.json();setPending(false);setMessage(response.ok?"Wachtwoord gewijzigd. Log opnieuw in.":data.error);if(response.ok)setTimeout(()=>location.assign("/login"),900)}return <form className="card card-pad form-stack" onSubmit={submit} style={{maxWidth:540,marginTop:20}}><div className="field"><label>Huidig wachtwoord</label><input className="input" type="password" name="currentPassword" autoComplete="current-password" required/></div><div className="field"><label>Nieuw wachtwoord</label><input className="input" type="password" name="newPassword" minLength={12} autoComplete="new-password" required/></div><button className="button button-primary" disabled={pending}>Wachtwoord wijzigen</button>{message&&<div className="small muted">{message}</div>}</form>}
