export type ScoreSignals={reachable:boolean;mobileScore:number|null;desktopScore:number|null;viewport:boolean|null;cta:boolean|null;form:boolean|null;placeholder:boolean|null;outdatedCopyright:boolean|null;brokenLinks:number;loadTimeMs:number|null};
export function scoreWebsite(signals:ScoreSignals){const reasons:{code:string;label:string;weight:number}[]=[];const add=(code:string,label:string,weight:number)=>reasons.push({code,label,weight});if(!signals.reachable)add("UNREACHABLE","Website niet bereikbaar",60);if(signals.mobileScore!==null&&signals.mobileScore<50)add("VERY_SLOW_MOBILE",`Mobiele score ${signals.mobileScore}/100`,22);else if(signals.mobileScore!==null&&signals.mobileScore<75)add("SLOW_MOBILE",`Mobiele score ${signals.mobileScore}/100`,12);if(signals.desktopScore!==null&&signals.desktopScore<60)add("SLOW_DESKTOP",`Desktopscore ${signals.desktopScore}/100`,8);if(signals.viewport===false)add("NOT_MOBILE_FRIENDLY","Website niet mobielvriendelijk",15);if(signals.cta===false)add("NO_CTA","Geen duidelijke call-to-action",10);if(signals.form===false)add("NO_CONTACT_FORM","Geen contactformulier",7);if(signals.placeholder)add("PLACEHOLDER","Placeholder of coming-soonpagina",28);if(signals.outdatedCopyright)add("OUTDATED_COPYRIGHT","Verouderde copyrightvermelding",7);if(signals.brokenLinks>0)add("BROKEN_LINKS",`${signals.brokenLinks} kapotte links gevonden`,Math.min(16,signals.brokenLinks*4));if((signals.loadTimeMs??0)>5000)add("VERY_SLOW_RESPONSE","Zeer trage serverreactie",12);const opportunityScore=Math.min(100,reasons.reduce((sum,r)=>sum+r.weight,0));const conversionQualityScore=Math.max(0,100-(signals.cta===false?25:0)-(signals.form===false?15:0)-(signals.viewport===false?25:0)-(signals.placeholder?30:0)-((signals.mobileScore??100)<50?20:0));return{opportunityScore,conversionQualityScore,reasons}}
